name: Reflaxe OCaml / Package Artifact Matrix

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/reflaxe-ocaml-package-matrix.yml'
      - 'haxelib.json'
      - 'package.json'
      - 'package-lock.json'
      - 'packages/reflaxe.ocaml/**'
      - 'scripts/ci/*reflaxe-ocaml-package*.js'
      - 'scripts/ci/*reflaxe-ocaml-*perf*.js'
      - 'scripts/release/**'
      - 'test/packaging/reflaxe_ocaml_external_app/**'
      - 'docs/00-project/REFLAXE_OCAML_PERF_BASELINE.json'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/reflaxe-ocaml-package-matrix.yml'
      - 'haxelib.json'
      - 'package.json'
      - 'package-lock.json'
      - 'packages/reflaxe.ocaml/**'
      - 'scripts/ci/*reflaxe-ocaml-package*.js'
      - 'scripts/ci/*reflaxe-ocaml-*perf*.js'
      - 'scripts/release/**'
      - 'test/packaging/reflaxe_ocaml_external_app/**'
      - 'docs/00-project/REFLAXE_OCAML_PERF_BASELINE.json'
  schedule:
    - cron: '41 6 * * 6'
  workflow_dispatch:

concurrency:
  group: reflaxe-ocaml-package-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
  NODE_VERSION: '20'
  HAXE_VERSION: '4.3.7'
  OCAML_VERSION: '5.2.1'
  DUNE_VERSION: '3.24.0'

jobs:
  package_artifact:
    name: Build immutable source package
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      - name: Cache Haxe toolchain (lix)
        uses: actions/cache@v5
        with:
          path: ~/haxe
          key: ${{ runner.os }}-lix-haxe-${{ env.HAXE_VERSION }}-${{ hashFiles('.haxerc', 'haxe_libraries/*.hxml', 'package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-lix-haxe-${{ env.HAXE_VERSION }}-
      - name: Install npm dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Install Haxe and Reflaxe dependencies
        run: |
          set -euo pipefail
          npx lix download
          npx lix download haxe "${HAXE_VERSION}"
          npx lix use haxe "${HAXE_VERSION}"
          echo "${PWD}/node_modules/.bin" >> "$GITHUB_PATH"
      - name: Build canonical package artifact
        run: npm run build:reflaxe-ocaml:package-artifact
      - name: Verify clean package provenance
        run: |
          set -euo pipefail
          node -e 'const m=require("./.artifacts/reflaxe-ocaml/package-artifact/artifact-manifest.json"); if(m.workingTreeDirty || m.implementationCommit !== process.env.GITHUB_SHA) throw new Error("package artifact is not tied to the clean workflow checkout"); console.log(`${m.marker} sha256=${m.package.sha256}`)'
      - name: Upload canonical package artifact
        uses: actions/upload-artifact@v6
        with:
          name: reflaxe-ocaml-source-package-${{ github.sha }}
          path: .artifacts/reflaxe-ocaml/package-artifact
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30

  package_install:
    name: Install exact package (${{ matrix.os }})
    needs: package_artifact
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Setup OCaml toolchain (OCaml ${{ env.OCAML_VERSION }})
        uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: ${{ env.OCAML_VERSION }}
          dune-cache: true
      - name: Install OCaml packages
        run: |
          set -euo pipefail
          eval "$(opam env)"
          opam install -y "dune.${DUNE_VERSION}" ocamlfind
          echo "${OPAM_SWITCH_PREFIX}/bin" >> "$GITHUB_PATH"
          dune --version
          ocamlc -version
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      - name: Cache Haxe toolchain (lix)
        uses: actions/cache@v5
        with:
          path: ~/haxe
          key: ${{ runner.os }}-lix-haxe-${{ env.HAXE_VERSION }}-${{ hashFiles('.haxerc', 'haxe_libraries/*.hxml', 'package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-lix-haxe-${{ env.HAXE_VERSION }}-
      - name: Install npm dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Install Haxe and Reflaxe dependencies
        run: |
          set -euo pipefail
          npx lix download
          npx lix download haxe "${HAXE_VERSION}"
          npx lix use haxe "${HAXE_VERSION}"
          echo "${PWD}/node_modules/.bin" >> "$GITHUB_PATH"
      - name: Download canonical package artifact
        uses: actions/download-artifact@v7
        with:
          name: reflaxe-ocaml-source-package-${{ github.sha }}
          path: .artifacts/reflaxe-ocaml/package-input
      - name: Select downloaded package paths
        run: |
          set -euo pipefail
          manifest="${PWD}/.artifacts/reflaxe-ocaml/package-input/artifact-manifest.json"
          archive_file="$(node -p 'require(process.argv[1]).package.archiveFile' "$manifest")"
          echo "RO_PACKAGE_INSTALL_MANIFEST=${manifest}" >> "$GITHUB_ENV"
          echo "RO_PACKAGE_INSTALL_ZIP=${PWD}/.artifacts/reflaxe-ocaml/package-input/${archive_file}" >> "$GITHUB_ENV"
          echo "RO_PACKAGE_INSTALL_ARTIFACTS=${PWD}/.artifacts/reflaxe-ocaml/package-install" >> "$GITHUB_ENV"
          echo "RO_PACKAGE_INSTALL_RETAIN_ROOT=${RUNNER_TEMP}/reflaxe-ocaml-package-install" >> "$GITHUB_ENV"
          echo "RO_PACKAGE_INSTALL_ENV_FILE=${RUNNER_TEMP}/reflaxe-ocaml-package-install/perf-env.json" >> "$GITHUB_ENV"
          echo "RO_PERF_MODE=platform-report" >> "$GITHUB_ENV"
          echo "RO_PERF_ENV_FILE=${RUNNER_TEMP}/reflaxe-ocaml-package-install/perf-env.json" >> "$GITHUB_ENV"
          echo "RO_PERF_ARTIFACT_MANIFEST=${manifest}" >> "$GITHUB_ENV"
          echo "RO_PERF_PACKAGE_INSTALL_SUMMARY=${PWD}/.artifacts/reflaxe-ocaml/package-install/summary.json" >> "$GITHUB_ENV"
          echo "RO_PERF_WORK_ROOT=${RUNNER_TEMP}/reflaxe-ocaml-perf-work" >> "$GITHUB_ENV"
          echo "RO_PERF_ARTIFACTS=${PWD}/.artifacts/reflaxe-ocaml/perf-platform" >> "$GITHUB_ENV"
      - name: Install and run the exact downloaded package
        run: npm run test:reflaxe-ocaml:package-install
      - name: Verify shared-artifact consumer result
        run: |
          set -euo pipefail
          node -e 'const s=require("./.artifacts/reflaxe-ocaml/package-install/summary.json"); if(s.marker !== "RO_PACKAGE_INSTALL_SMOKE:PASS" || s.package.buildMode !== "supplied" || s.package.producerCommit !== process.env.GITHUB_SHA) throw new Error("platform job did not validate the shared package artifact"); console.log(`RO_PACKAGE_ARTIFACT_CONSUMER:PASS os=${process.platform} sha256=${s.package.sha256}`)'
      - name: Measure the installed package on this host
        run: npm run test:reflaxe-ocaml:perf-platform
      - name: Verify host performance receipt
        run: |
          set -euo pipefail
          node -e 'const s=require("./.artifacts/reflaxe-ocaml/perf-platform/summary.json"); if(s.schemaVersion !== 2 || s.marker !== "RO_TARGET_PERF_PLATFORM:PASS" || s.method?.id !== "installed-package-platform-v2" || s.iteration?.passed !== true || !s.provenance.installedPackageProof || s.provenance.implementationCommit !== process.env.GITHUB_SHA) throw new Error("host performance did not measure the shared installed package with the v2 iteration method"); console.log(`${s.marker} os=${s.environment.platform} package=${s.provenance.package.sha256}`)'
      - name: Upload platform package evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: reflaxe-ocaml-package-install-${{ runner.os }}-${{ github.sha }}
          path: .artifacts/reflaxe-ocaml/package-install
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 30
      - name: Upload host performance evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: reflaxe-ocaml-perf-${{ runner.os }}-${{ github.sha }}
          path: .artifacts/reflaxe-ocaml/perf-platform
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 30

  package_matrix_summary:
    name: Verify Linux and macOS package matrix
    needs: [package_artifact, package_install]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          package-manager-cache: false
      - name: Download canonical package artifact
        uses: actions/download-artifact@v7
        with:
          name: reflaxe-ocaml-source-package-${{ github.sha }}
          path: .artifacts/reflaxe-ocaml/package-input
      - name: Download platform package evidence
        uses: actions/download-artifact@v7
        with:
          pattern: reflaxe-ocaml-package-install-*-${{ github.sha }}
          path: .artifacts/reflaxe-ocaml/package-consumers
          merge-multiple: false
      - name: Verify shared package identity and both hosts
        run: |
          set -euo pipefail
          node scripts/ci/reflaxe-ocaml-package-matrix-summary.js \
            --artifact-manifest .artifacts/reflaxe-ocaml/package-input/artifact-manifest.json \
            --evidence-root .artifacts/reflaxe-ocaml/package-consumers \
            --out .artifacts/reflaxe-ocaml/package-matrix/summary.json
      - name: Upload verified package matrix
        uses: actions/upload-artifact@v6
        with:
          name: reflaxe-ocaml-package-matrix-${{ github.sha }}
          path: .artifacts/reflaxe-ocaml/package-matrix
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30

  package_performance_summary:
    name: Verify Linux and macOS package performance evidence
    needs: [package_artifact, package_install]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          package-manager-cache: false
      - name: Download canonical package artifact
        uses: actions/download-artifact@v7
        with:
          name: reflaxe-ocaml-source-package-${{ github.sha }}
          path: .artifacts/reflaxe-ocaml/package-input
      - name: Download host performance evidence
        uses: actions/download-artifact@v7
        with:
          pattern: reflaxe-ocaml-perf-*-${{ github.sha }}
          path: .artifacts/reflaxe-ocaml/perf-consumers
          merge-multiple: false
      - name: Verify shared package identity, method, output, and raw samples
        run: |
          set -euo pipefail
          node scripts/ci/reflaxe-ocaml-perf-matrix-summary.js \
            --artifact-manifest .artifacts/reflaxe-ocaml/package-input/artifact-manifest.json \
            --evidence-root .artifacts/reflaxe-ocaml/perf-consumers \
            --out .artifacts/reflaxe-ocaml/perf-matrix/summary.json
      - name: Upload verified package performance matrix
        uses: actions/upload-artifact@v6
        with:
          name: reflaxe-ocaml-perf-matrix-${{ github.sha }}
          path: .artifacts/reflaxe-ocaml/perf-matrix
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 30

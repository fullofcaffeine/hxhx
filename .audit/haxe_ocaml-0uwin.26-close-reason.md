Selected and independently reviewed the occurrence-level runtime-reference
authority design. The checkpoint reproduces the same-symbol blind spot,
defines the sealed per-use record and restricted target-identifier provenance,
specifies final structured/generated-text/raw reconciliation and complete
authority gates, and creates bounded implementation children. This closes the
design decision only; runtime authority remains partial and README goals are
unchanged.

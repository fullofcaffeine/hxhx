Occurrence authority and source selection are separate proofs. Shadow mode
computes the requirements-only closure, compares it with the current selected
closure and real compile/build/run results, and reports differences without
changing output. The hard cut is allowed only after the legacy inventory is
zero and all target references reconcile to direct requirement roots.

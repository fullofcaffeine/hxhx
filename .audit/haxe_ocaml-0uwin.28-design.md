The lowered plan owns semantic meaning and creates one immutable use occurrence
for the array store. The target syntax carries only use ID, plan revision,
domain, and exact target symbol. The printer ignores provenance and renders the
same OCaml bytes as before.

Use narrow expression/type/pattern identifier forms rather than a general
expression annotation wrapper. The final reconciler compares sealed uses with
the completed structured syntax and verifies owner-local planned order where
the lowered plan declares one.

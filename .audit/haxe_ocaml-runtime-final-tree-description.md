## User-visible outcome

The OCaml target rejects a private runtime helper that appears more times in
the complete generated program than its lowering decisions authorized, even
when compiler code reuses one already-checked syntax subtree in several output
locations.

## Scope

Add one final structured-output reconciliation boundary after modules and
support files have been assembled but before printing or publication. Preserve
the exact hidden occurrence identities carried by `ERuntimeIdent` and checked
generated-text placeholders. Compare the complete owner/order/cardinality set
against the union of sealed request-local runtime-use plans without using
rendered target text as permission.

Cover deliberate compiler duplication such as normal versus empty instance
constructors and normal constructor bodies versus dispatch constructor bodies.
Reject missing, duplicated, reordered, stale, wrong-owner, wrong-symbol,
wrong-profile, and unplanned private references. Keep plan-local reconciliation
for focused diagnostics; the final pass is an additional whole-output proof.

Do not broaden runtime requirements, authorize legacy inventory rows, change
Haxe behavior, or claim complete runtime source selection until every inventory
family and this final multiplicity check are closed.

## Acceptance criteria

A focused fixture proves that reusing one reconciled subtree twice fails unless
two exact output occurrences were planned; the completed generated module set
reconciles every structured and checked-text private runtime reference exactly
once against the sealed request inventory; failure occurs before printing or
publication; existing compile/build/run behavior remains unchanged; runtime
authority and README readiness remain partial until the parent acceptance is
otherwise complete.

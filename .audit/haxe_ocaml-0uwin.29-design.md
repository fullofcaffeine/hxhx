Sealing checked generated text records its plan revision, ordered use IDs,
exact bytes, and content hash. Literal text cannot name the private runtime
namespace. Placeholders consume sealed occurrences and render exact symbols.
The generated-text record is validated before output publication; scanning
rendered text remains only a negative guard.

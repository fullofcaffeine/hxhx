## User-visible outcome

Selective runtime packaging can eventually choose its files from explicit
semantic requirements alone, without compiler-observed module names acting as
hidden extra roots.

## Scope

First run requirements-only selection in shadow mode across the complete
supported matrix while the existing selection remains authoritative. After all
occurrence, generated-text, raw, and legacy-inventory gates are complete, make
the hard cut and remove observation roots from correctness.

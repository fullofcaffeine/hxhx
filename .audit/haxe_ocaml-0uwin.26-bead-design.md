Compare at least these designs against current source:

1. a checked runtime-reference factory that emits plain target syntax and an
   exact request-local use receipt;
2. lowered runtime-use plans with deterministic occurrence identities and
   expected target symbols, followed by syntax reconciliation;
3. object-identity linkage between final syntax nodes and requirement records;
4. a syntax-level reference type or wrapper; and
5. keeping module-name comparison as permanent authority.

Reject any design that makes printed text authoritative, adds semantic
annotations to `OcamlExpr`, creates a permanent disconnected side table,
allows raw runtime references without declarations, or turns compiler policy
into a blanket explanation for unrelated Haxe behavior.

Use a focused GPT-5.6 Pro Oracle architecture review because the accepted
2026-07-18 review defines the ownership direction but not this final binding
seam. Reconcile every material recommendation against current `main` before
creating implementation children.

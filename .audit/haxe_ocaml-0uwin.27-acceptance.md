1. A deterministic inventory covers structured expression/type/pattern references, generated text, and raw boundaries.
2. The current non-zero baseline is recorded without changing generated output or README goals.
3. Adding a new plain private-runtime constructor makes the focused guard fail with owner and symbol information.
4. Removing or migrating an entry requires reviewed inventory regeneration; silent snapshot refresh is rejected.
5. The inventory groups follow-up work into bounded semantic families and names mega-file extraction risks.
6. Focused guard tests and clean-repeat determinism pass.

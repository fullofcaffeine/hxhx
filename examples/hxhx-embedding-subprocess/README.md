# hxhx embedding subprocess fixture

This fixture is used by:

- `npm run hxhx:example:embedding-subprocess`

It provides:

- `Main.hx`: successful compile path (report capture)
- missing-main compile invocation (`-main MissingMain`) for deterministic diagnostic capture

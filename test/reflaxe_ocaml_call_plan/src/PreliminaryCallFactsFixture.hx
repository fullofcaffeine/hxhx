/** Supplies ordinary typed calls for on-demand preliminary-planning checks. */
class PreliminaryCallFactsFixture {
	/** Returns the exact String argument without changing its carrier. */
	public static function identity(value:String):String {
		return value;
	}
}

# 1 "backend_source_LuaERegRuntime.ml"
(* Generated by reflaxe.ocaml (WIP) *)
(* Haxe type: backend.source.LuaERegRuntime *)

let __reflaxe_ocaml__ = ()

type t = { __hx_type : Obj.t }

let create = fun () -> let self = ({ __hx_type = HxType.class_ "backend.source.LuaERegRuntime" } : t) in (
  ignore ();
  self
)

let __empty = fun () -> ({ __hx_type = HxType.class_ "backend.source.LuaERegRuntime" } : t)

let hx_SOURCE = ("-- hxhx Lua runtime shim: focused EReg surface.\nEReg = EReg or {}\nEReg.new = EReg.new or function(pattern, options)\n  local self = {\n    pattern = tostring(pattern or \"\"),\n    options = tostring(options or \"\")\n  }\n  self.match = function(value)\n    local text = tostring(value or \"\")\n    local haystack = text\n    local needle = self.pattern\n    if string.find(self.options, \"i\", 1, true) ~= nil then\n      haystack = string.lower(haystack)\n      needle = string.lower(needle)\n    end\n    return string.find(haystack, needle, 1, true) ~= nil\n  end\n  return self\nend\nEReg.create = EReg.create or EReg.new" : string)

let lines = fun () -> HxString.split hx_SOURCE "\n"